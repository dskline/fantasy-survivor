{
  "name": "fantasy-survivor",
  "version": "0.1.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "build:dev": "doppler run -- turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "prepare": "husky install"
  },
  "devDependencies": {
    "turbo": "1.4.6",
    "husky": "8.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
