## @fantasy-survivor/src/utilities/graphql

This folder is auto generated using `gqless-cli` via the `@fantasy-survivor/database` repo.

If this folder is moved, the new location should be reflected in `@fantasy-survivor/database/gqless.config.ts`
